---
title: "Markdown"
date: '2023-03-24'
slug: Markdown
categories:
  - extras
tags:
subtitle: ''
summary: ''
authors: [pommeranz]
lastmod: '`r Sys.Date()`'
featured: no
header:
  image: "/header/" #https://pxhere.com/de/photo/29 maybe this?
  caption: "[Courtesy of pxhere](https://pxhere.com/de/photo/29)"
projects: []
---

`r blogdown::shortcode_open("detail_disclosure", "Kernfragen dieses Beitrags")`
<!-- Die divs sind wichtig, damit die Liste funktioniert! Innerhalb dieses Shortcodes ist HTML erlaubt (safeHTML) -->
<div>
* Was ist [**Markdown**](#Markdown), wofür [**brauche**](#UseCase) ich das?
* Wie formatiere ich [**Textsegmente**](#Textsegmente) in Markdown?
* Wie erstelle ich [**Headings**](#Heading) und [**Tabellen**](#Tabellen) in Markdown?
* Wie erstelle ich [**Blockzitate**](#Blockzitat) und [**Listen**](#Listen) in Markdown?
* Was sind [**Codeblöcke**](#Codeblock) und wie mache ich [**Trennstriche**](#Trennstrich) in Markdown?
* Wie funktioniert das [**Working Directory**](#WD) im Zusammenhang mit Markdown?
* Wie binde ich [**Bilder**](#Bilder) und [**interne Links**](#InternalLinks) in Markdown ein?
* Welchen Nutzen hat Markdown im [**erweiterten Sinne**](#Advanced)?
</div>

`r blogdown::shortcode_close("detail_disclosure")`

***

Im Folgenden Tutorial wird euch allgemeines Markdown nähergebracht, mit dem rein textbasierte Arbeiten etwas dynamischer und visuell ansprechender gestaltet werden können. Dazu gibt es ein relevantes Anwendungsbeispiel das näherbringen soll wie sich Markdown auf die eigentliche wissenschaftliche Arbeit anwenden lassen kann.

## Was ist Markdown? {#Markdown .anchorheader}

Markdown ist im allgemeineren eine Auszeichnungssprache, die in fast jeder textbasierten Anwendung in relativ ähnlicher Form vorkommt, um spezifische Textdarstellungen und Inhalte in Texten zu ermöglichen. Es erinnert dabei mehr an eine Codingsprache wie R als an einen schön aufbereiteter Editor wie Word. Ein Beispiel dafür ist die *Inline Formatierung*. Mit ihr kann man beispielsweise mit  `**Text**`  **Text fett schreiben** oder auch per  `~~Text~~`  ~~Text durchstreichen~~. Dies kann man meist in vergleichbarer Form auch in einem Messenger der Wahl replizieren. In WhatsApp lässt sich Text zum Beispiel mit `~Text~` durchstreichen. Man kann auch [Links]() einbinden, indem man den gewollten Textteil in `[]` Klammern einschließt und den entsprechenden Link in `()` Klammern setzt. Man kann auch intern auf die jeweiligen Abschnitte verlinken, indem man diese mit einem Link versieht, dazu mehr unter [interne Links](#InternalLinks). Markdown-Dateien enden auf .md und können auch innerhalb von RStudio bearbeitet werden. Diese können direkt in RStudio erstellt werden, dafür muss man auf "File", dann "New File" gehen und weiter unten "Markdown File" auswählen.

<!-- Ein paar Beispiele fü die Funktionalität sind schon eine sehr gute Idee, auch der Bezug zu WhatsApp. Aber dann werden es ein paar zu viel Sachen - vielleicht das mit den Links an dieser Stelle hier rausnehmen. -->
<!-- Das Erstellen von Dateien würde ich erst angehen, wenn auch wirklich die Arbeit startet - also nach Wofür Markdown und das dann auch als Setup bezeichnen. Da kannst du dann auch das Beispiel aufführen; das kommt irgendwie etwas aus dem Nichts dann-->

<!--- Bild hier einfügen? -->

[]: # (Bild hier einfügen?)

## Wofür Markdown? {#UseCase .anchorheader}

Ein möglicher Einsatz der Formatierung von Markdown und später auch RMarkdown kann sinnvoll sein, wenn man Informationen, etwa wissenschaftliche Forschung oder sonstige studentische Arbeiten auf Websiten und PDFs mit einer Reihe zusätzlicher Funktionen darstellen möchte. Der große Gewinn gegenüber anderen Textformatierungs- und Textverarbeitungsprogrammen wie Word ist das Markdown universell kompilierbar, kostenlos und offen zugänglich ist, womit weder Schreiber noch Leser je an eine spezifische Software und den damit verbundenen Kosten oder der Instandhaltung dieser Software gebunden ist. 

<!-- Weiterer großer Vorteil, wenn du hier auch schon RMarkdown erwähnst- direkte Einbindung von RCode anstatt Screenshots -->

Innerhalb dieser Tutorial-Serie werden wir Markdown und Erweiterungen dessen Nutzen, um einen Mini-Bericht zu einer kleinen Auswertung anzufertigen.

## Textbestandteile {#Textsegmente .anchorheader}

Es gibt in Markdown auch typische Bestandteile zur visuellen Trennung des Text, wie man sie aus anderen Dokumentbearbeitungsprogrammen auch gewohnt ist. Darunter fallen Zeilenumbrüche und neue Paragraphen. Diese beiden werden auch, wenn man Markdown nutzt um eine Website zu schreiben, zu den entsprechenden HTML-Gegenstücken kompiliert, `<p></p> für Paragraphen, <br> für Zeilenumbrüche`.

Ein neuer Paragraph wird erstellt, indem man eine Zeile oberhalb und unterhalb des Text freilässt.

Ein Zeilenumbruch wird in Markdown erstellt, indem man zwei Leerzeichen an das Ende einer Zeile setzt und dann per Enter eine neue Zeile beginnt. In dem Fall von Markdown per RStudio braucht man auch keine Leerstellen, aber diese sind insgesamt typisch für allgemeines Markdown und fallen nicht auf, auch wenn sie überschüssig sind.

Unser reiner Text für die Auswertung einer linearen Regression aus dem [fb22-Datensatz]() würde nun so aussehen:

<!--- Version 1 hier einfügen? -->

[]: # (Bild hier einfügen?)

`r blogdown::shortcode_open("detail_disclosure", "Hier klicken für den entsprechenden Code")`
<pre>
**Version 1 des Miniberichts**

**Deskriptiv**

Von der Stichprobe dieses Fragebogen wurde nur die Geschlechterverteilung aufgenommen,

**Was wurde durchgeführt?**

Es wurde eine lineare Regression durchgeführt, um zu sehen, ob innerhalb des Datenatz die Nerdigkeit einen linearen Zusammenhang mit der Neurotizität aufzeigt. 


**Vorraussetzungsprüfung**

Vorrausssetzungsprüfungen wurden graphisch durchgeführt und waren alle annehmbar, wobei die Normalverteilung noch zusätzlich mit einem shapiro Test (p = .0698) überprüft wurde, um die Voraussetzung sicher annehmen zu können.

**Ergebnis**

Das Ergebnis ist, dass unser Prädiktor nicht signifikant von Null verschieden ist (p-Wert von 0.3406 bei einem Alpha von .05).
</pre>
`r blogdown::shortcode_close("detail_disclosure")`

<!-- Vielleicht noch nicht die ganzen Ergebnisse aus R-Berechnungen hier einbauen sondern nur Text? -->

## Heading {#Heading .anchorheader}

Ein typischer Bestandteil eines Dokuments sind die Headings, diese werden in Markdown mit Rauten entsprechend des Heading-Level vor dem Wort erstellt.

| Code | Level |
|---|---|
| `# Heading`  | 1 |
| `## Heading`  |  2 |
| `### Heading`  |  3 |
| `#### Heading`  |  4 |
| `##### Heading`  | 5  |
| `###### Heading`  | 6  |

Es ist wichtig, dass die Headings am Anfang der Zeile stehen und das zwischen Rauten und Titel des Headings eine Leerstelle ist, da sie sonst nicht funktionieren. Leerraum zwischen Headings und dem restlichen Text sind auch erwünscht, da sie sonst auch unter Umständen nicht funktionieren.

Hiermit können wir unserem Text Struktur geben!

<!-- Struktur gab es ja theoretisch schon durch das Fette schreiben - aber vielleicht auch darüber hinaus Struktur geben und dann auch später Verweise erstellen können -->

<!--- Version 2 hier einfügen? -->

[]: # (Bild hier einfügen?)

Um den Lerneffekt zu maximieren, empfiehlt es sich für die nachfolgenden Schritte zuerst selbst den Code anzupassen, um das im Screenshot dargestellte Ergebnis zu erreichen!

`r blogdown::shortcode_open("detail_disclosure", "Zeig mir die Lösung an")`
<pre>
## Version 2 des Miniberichts

#### **Deskriptiv** 

Von der Stichprobe dieses Fragebogen wurde nur die Geschlechterverteilung aufgenommen.

#### **Was wurde durchgeführt?** 

Es wurde eine lineare Regression durchgeführt, um zu sehen, ob innerhalb des Datenatz die Nerdigkeit einen linearen Zusammenhang mit der Neurotizität aufzeigt. 


#### **Vorraussetzungsprüfung** 

Vorrausssetzungsprüfungen wurden graphisch durchgeführt und waren alle annehmbar, wobei die Normalverteilung noch zusätzlich mit einem shapiro Test (p = .0698) überprüft wurde, um die Voraussetzung sicher annehmen zu können.

#### **Ergebnis**

Das Ergebnis ist, dass unser Prädiktor nicht signifikant von Null verschieden ist (p-Wert von 0.3406 bei einem Alpha von .05).
</pre>
`r blogdown::shortcode_close("detail_disclosure")`

## Tabellen {#Tabellen .anchorheader}

Tabellen lassen sich auch per Markdown rein textbasiert erstellen, so wie es auch bei den Headings zuvor gemacht wurde

```
| Spalte 1 | Spalte 2 |
|---|---|
| Spalte 1, Eintrag 1  | Spalte 2, Eintrag 1 |
| Spalte 1, Eintrag 2  | Spalte 2, Eintrag 2 |

```

wird dargestellt als

| Spalte 1 | Spalte 2 |
|---|---|
| Spalte 1, Eintrag 1  | Spalte 2, Eintrag 1 |
| Spalte 1, Eintrag 2  | Spalte 2, Eintrag 2 |

Beispielsweise können wir unsere deskriptiven Ergebnisse in einer Tabelle darstellen:

<!-- Dafür gibt es ja später theoretisch eigene Befehle in R - gibt es hier vielleicht einen anderen Einsatzgrund, den man sich vorstellen könnte? -->

<!--- Version 3 hier einfügen? -->

[]: # (Bild hier einfügen?)

`r blogdown::shortcode_open("detail_disclosure", "Zeig mir die Lösung an")`
<pre>
## Version 3 des Miniberichts

#### **Deskriptiv** 

Von der Stichprobe dieses Fragebogen wurde nur die Geschlechterverteilung aufgenommen, welche folgendermaßen aussieht:

| Weiblich | Männlich | Anderes |
|---|---|---|
| 125  | 23 | 1 |


#### **Was wurde durchgeführt?** 

Es wurde eine lineare Regression durchgeführt, um zu sehen, ob innerhalb des Datenatz die Nerdigkeit einen linearen Zusammenhang mit der Neurotizität aufzeigt. 


#### **Vorraussetzungsprüfung** 

Vorrausssetzungsprüfungen wurden graphisch durchgeführt und waren alle annehmbar, wobei die Normalverteilung noch zusätzlich mit einem shapiro Test (p = .0698) überprüft wurde, um die Voraussetzung sicher annehmen zu können.

#### **Ergebnis**

Das Ergebnis ist, dass unser Prädiktor nicht signifikant von Null verschieden ist (p-Wert von 0.3406 bei einem Alpha von .05).
</pre>
`r blogdown::shortcode_close("detail_disclosure")`

## Blockzitat {#Blockzitat .anchorheader}

Per `>` zu Beginn jeder Zeile lassen sich Blockzitate erstellen, welche wie folgt aussehen:
> Dies ist ein Blockzitat
>
> Sie können auch über mehrere Zeilen gehen, in einem *Block*
>> Ein Blockzitat kann sich auch in einem Blockzitat befinden

<!-- Hier schon klar machen, wofür wir das gleich verwenden würden -->

## Listen {#Listen .anchorheader}

Sowohl sortierte Listen mit Zahlen als auch unsortierte Strichlisten werden automatisch in Markdown konvertiert

```
1. Eins
2. Zwei
3. Drei

- Eins
- Zwei
- Drei
```

wird zu

1. Eins
2. Zwei
3. Drei

- Eins 
- Zwei
- Drei

Die Listen und Blockzitate können wir nun beispielsweise Nutzen um unsere Null- und Alternativhypothese hervorzuheben:

<!--- Version 4 hier einfügen? -->

[]: # (Bild hier einfügen?)

`r blogdown::shortcode_open("detail_disclosure", "Zeig mir die Lösung an")`
<pre>
## Version 4 des Miniberichts

#### **Deskriptiv** 

Von der Stichprobe dieses Fragebogen wurde nur die Geschlechterverteilung aufgenommen, welche folgendermaßen aussieht:

| Weiblich | Männlich | Anderes |
|---|---|---|
| 125  | 23 | 1 |


#### **Was wurde durchgeführt?** 

Es wurde eine lineare Regression durchgeführt, um zu sehen, ob innerhalb des Datenatz die Nerdigkeit einen linearen Zusammenhang mit der Neurotizität aufzeigt.  

Als Hypothesen formuliert:

>
>Hypothesen
>
> - H1: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist statistisch bedeutsam. 
>
> - H0: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist nicht signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist nicht statistisch bedeutsam. 
>

#### **Vorraussetzungsprüfung** 

Vorrausssetzungsprüfungen wurden graphisch durchgeführt und waren alle annehmbar, wobei die Normalverteilung noch zusätzlich mit einem shapiro Test (p = .0698) überprüft wurde, um die Voraussetzung sicher annehmen zu können.

#### **Ergebnis**

Das Ergebnis ist, dass unser Prädiktor nicht signifikant von Null verschieden ist (p-Wert von 0.3406 bei einem Alpha von .05). Die Alternativhypothese wird verworfen und die Nullhypothese bleibt erhalten.
</pre>
`r blogdown::shortcode_close("detail_disclosure")`

## Codeblöcke {#Codeblock .anchorheader}

Mit Backticks ` ` ` lassen sich Code-Inhalte anzeigen, mit jeweils einem Backtick um ein Wort wird nur das einzelne Wort Inline als Code dargestellt. Wenn man einen Textabschnitt um ein Tab einrückt oder jeweils drei Backticks unter und über einen Block packt, wird der ganze Block als Code dargestellt. Damit wurden auch zuvor die einzelnen Operatoren aufgeführt, bevor sie in ihrer Anwendung gezeigt wurden.

Damit können wir bereits unseren Code nachverfolgbar darstellen, den wir genutzt haben um unsere lineare Regression durchzuführen:

<!--- Version 5 hier einfügen? -->

[]: # (Bild hier einfügen?)

`r blogdown::shortcode_open("detail_disclosure", "Zeig mir die Lösung an")`
<pre>
## Version 5 des Miniberichts

#### **Datensatz**
Der Datensatz kann mit folgendem Befehl geladen werden:
```
load(url('https://pandar.netlify.app/post/fb22.rda')) 
```

#### **Deskriptiv** 

Von der Stichprobe dieses Fragebogen wurde nur die Geschlechterverteilung aufgenommen, welche folgendermaßen aussieht:

| Weiblich | Männlich | Anderes |
|---|---|---|
| 125  | 23 | 1 |

Der Code dazu sah folgenderweise aus:
```
fb22$geschl_faktor <- factor(fb22$geschl
                             ,levels = 1:3,
                             labels = c("weiblich"
                                        ,
                                        "männlich", "anderes"))
table(fb22$geschl_faktor)
```

#### **Was wurde durchgeführt?** 

Es wurde eine lineare Regression durchgeführt, um zu sehen, ob innerhalb des Datenatz die Nerdigkeit einen linearen Zusammenhang mit der Neurotizität aufzeigt.  

Als Hypothesen formuliert:

>
>Hypothesen
>
> - H1: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist statistisch bedeutsam. 
>
> - H0: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist nicht signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist nicht statistisch bedeutsam. 
>

Der Code dazu sah folgenderweise aus:
```
model <- lm(neuro ~ nerd, fb22)
```

#### **Vorraussetzungsprüfung** 

Vorrausssetzungsprüfungen wurden graphisch durchgeführt und waren alle annehmbar, wobei die Normalverteilung noch zusätzlich mit einem shapiro Test (p = .0698) überprüft wurde, um die Voraussetzung sicher annehmen zu können.

Der Code dazu sah folgenderweise aus:
```
UV <- fb22$nerd
AV <- fb22$neuro

## Linearität
plot(UV, AV, xlab = "Nerdigkeit", ylab = "Neurozität", 
     main= "Zusammenhang zwischen Gewicht und Treibstoffeffizienz"
     , pch =19)
lines(loess.smooth(UV, AV), col = 'blue')

## Varianzhomogenität
# Siehe Plot
par(mfrow = c(2, 2)) #vier Abbildungen gleichzeitig
plot(model)

## Normalverteilung der Residuen
shapiro.test(model[["residuals"]])
# +plot

## Unabhängigkeit der Residuen
# Siehe plot

```

#### **Ergebnis**

Das Ergebnis ist, dass unser Prädiktor nicht signifikant von Null verschieden ist (p-Wert von 0.3406 bei einem Alpha von .05). Die Alternativhypothese wird verworfen und die Nullhypothese bleibt erhalten.

Der Code dazu sah folgenderweise aus:
```
summary(model)$r.squared
p = 0.3406
p < 0.05
```
</pre>
`r blogdown::shortcode_close("detail_disclosure")`

## Horizontale Trennstriche {#Trennstrich .anchorheader}

Mit drei Asterisks *** oder Underscores ___ kann man eine horizontale Linie als Trenner einbauen. Diese müssen dafür für sich alleine in einer eigenen Zeile stehen und sollten auch eine Zeile Abstand zu restlichem Text haben, da sie sonst zu einem Heading mit den vorherstehenden Inhalten kombiniert werden. Man kann dabei auch beliebig oft die Zeichen wiederholen, statt nur drei mal, sofern man den Code selbst auch visuell trennen möchte.

Mit den Strichen lassen sich beispielsweise die Codeabschnitte besser visuell trennen.

<!-- Finde diesen Einsatz nicht so intuitiv. Ist ja eher für die Trennung von sehr unterschiedlichen Blocks oder? Vielleicht können wir die Hypothesen in der Einleitung von den Berechnungen trennen oder so -->

<!--- Version 6 hier einfügen? -->

[]: # (Bild hier einfügen?)

`r blogdown::shortcode_open("detail_disclosure", "Zeig mir die Lösung an")`
<pre>
## Version 6 des Miniberichts

#### **Datensatz**
Der Datensatz kann mit folgendem Befehl geladen werden:
```
load(url('https://pandar.netlify.app/post/fb22.rda')) 
```

#### **Deskriptiv** 

Von der Stichprobe dieses Fragebogen wurde nur die Geschlechterverteilung aufgenommen, welche folgendermaßen aussieht:

| Weiblich | Männlich | Anderes |
|---|---|---|
| 125  | 23 | 1 |

***

Der Code dazu sah folgenderweise aus:
```
fb22$geschl_faktor <- factor(fb22$geschl
                             ,levels = 1:3,
                             labels = c("weiblich"
                                        ,
                                        "männlich", "anderes"))
table(fb22$geschl_faktor)
```

#### **Was wurde durchgeführt?** 

Es wurde eine lineare Regression durchgeführt, um zu sehen, ob innerhalb des Datenatz die Nerdigkeit einen linearen Zusammenhang mit der Neurotizität aufzeigt.  

Als Hypothesen formuliert:

>
>Hypothesen
>
> - H1: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist statistisch bedeutsam. 
>
> - H0: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist nicht signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist nicht statistisch bedeutsam. 
>

***

Der Code dazu sah folgenderweise aus:
```
model <- lm(neuro ~ nerd, fb22)
```

#### **Vorraussetzungsprüfung** 

Vorrausssetzungsprüfungen wurden graphisch durchgeführt und waren alle annehmbar, wobei die Normalverteilung noch zusätzlich mit einem shapiro Test (p = .0698) überprüft wurde, um die Voraussetzung sicher annehmen zu können.

***

Der Code dazu sah folgenderweise aus:
```
UV <- fb22$nerd
AV <- fb22$neuro

## Linearität
plot(UV, AV, xlab = "Nerdigkeit", ylab = "Neurozität", 
     main= "Zusammenhang zwischen Gewicht und Treibstoffeffizienz"
     , pch =19)
lines(loess.smooth(UV, AV), col = 'blue')

## Varianzhomogenität
# Siehe Plot
par(mfrow = c(2, 2)) #vier Abbildungen gleichzeitig
plot(model)

## Normalverteilung der Residuen
shapiro.test(model[["residuals"]])
# +plot

## Unabhängigkeit der Residuen
# Siehe plot

```

#### **Ergebnis**

Das Ergebnis ist, dass unser Prädiktor nicht signifikant von Null verschieden ist (p-Wert von 0.3406 bei einem Alpha von .05). Die Alternativhypothese wird verworfen und die Nullhypothese bleibt erhalten.

***

Der Code dazu sah folgenderweise aus:
```
summary(model)$r.squared
p = 0.3406
p < 0.05
```
</pre>
`r blogdown::shortcode_close("detail_disclosure")`

## Working Directory {#WD .anchorheader}

Das Working Directory ist bei plain Markdown generell der Ordner, in dem sich das Markdown-Dokument befindet. Dieses brauchen wir für den nächsten Schritt, wenn wir externe Dateien einbinden wollen.


##Bilder einbinden {#Bilder .anchorheader}

Per
`![Bildbeschreibung](Ordnerpfad, in dem sich Bild befindet, ausgehend von dem Ort an dem .md gespeichert ist)` lässt sich auch ein Bild in den Text einbinden, jedoch lässt sich dieses nicht direkt in seinen Dimensionen oder ähnlichem anpassen.

<!-- Gibt es auch Bilder, die wird unabhängig von den Ergebnissen uns vorstellen könnten? Wir sollten die Bilder auf jeden Fall auch als Download zur Verfügung stellen, falls Leute parallel wirklich mitarbeiten -->

<!--- Version 7 hier einfügen? -->

[]: # (Bild hier einfügen?)

Hinweis: Der Ordner heißt `Plots_Bilder`, das erste Bild `Linearity` und das Zweite `PlotsOverview`.

`r blogdown::shortcode_open("detail_disclosure", "Zeig mir die Lösung an")`
<pre>
## Version 7 des Miniberichts

#### **Datensatz**
Der Datensatz kann mit folgendem Befehl geladen werden:
```
load(url('https://pandar.netlify.app/post/fb22.rda')) 
```

#### **Deskriptiv** 

Von der Stichprobe dieses Fragebogen wurde nur die Geschlechterverteilung aufgenommen, welche folgendermaßen aussieht:

| Weiblich | Männlich | Anderes |
|---|---|---|
| 125  | 23 | 1 |

***

Der Code dazu sah folgenderweise aus:
```
fb22$geschl_faktor <- factor(fb22$geschl
                             ,levels = 1:3,
                             labels = c("weiblich"
                                        ,
                                        "männlich", "anderes"))
table(fb22$geschl_faktor)
```

#### **Was wurde durchgeführt?** 

Es wurde eine lineare Regression durchgeführt, um zu sehen, ob innerhalb des Datenatz die Nerdigkeit einen linearen Zusammenhang mit der Neurotizität aufzeigt.  

Als Hypothesen formuliert:

>
>Hypothesen
>
> - H1: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist statistisch bedeutsam. 
>
> - H0: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist nicht signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist nicht statistisch bedeutsam. 
>

***

Der Code dazu sah folgenderweise aus:
```
model <- lm(neuro ~ nerd, fb22)
```

#### **Vorraussetzungsprüfung** 

Die Vorrausssetzungsprüfungen wurden graphisch durchgeführt.

Die Voraussetzung der Linearität:

![Voraussetzung 1](Plots_Bilder/Linearity.png)

Die restlichen Vorraussetzungsprüfungen:

![Voraussetzung 2-4](Plots_Bilder/Plots_Overview.png)

Sie sind alle annehmbar, wobei die Normalverteilung noch zusätzlich mit einem shapiro Test (p = .0698) überprüft wurde, da der dargestellte QQ-Plot schon an der Grenze zu liegen scheint.

***

Der Code dazu sah folgenderweise aus:
```
UV <- fb22$nerd
AV <- fb22$neuro

## Linearität
plot(UV, AV, xlab = "Nerdigkeit", ylab = "Neurozität", 
     main= "Zusammenhang zwischen Gewicht und Treibstoffeffizienz"
     , pch =19)
lines(loess.smooth(UV, AV), col = 'blue')

## Varianzhomogenität
# Siehe Plot
par(mfrow = c(2, 2)) #vier Abbildungen gleichzeitig
plot(model)

## Normalverteilung der Residuen
shapiro.test(model[["residuals"]])
# +plot

## Unabhängigkeit der Residuen
# Siehe plot

```

#### **Ergebnis**

Das Ergebnis ist, dass unser Prädiktor nicht signifikant von Null verschieden ist (p-Wert von 0.3406 bei einem Alpha von .05). Die Alternativhypothese wird verworfen und die Nullhypothese bleibt erhalten.

***

Der Code dazu sah folgenderweise aus:
```
summary(model)$r.squared
p = 0.3406
p < 0.05
```
</pre>
`r blogdown::shortcode_close("detail_disclosure")`

## Interne Links {#InternalLinks .anchorheader}

Interne Links referenzieren Heading in dem Text. Dafür muss man hinter den jeweiligen Heading eine Leerstelle einbauen und per {#Text} eine Referenz für den internen Link einbauen. Nun baut man wie zuvor erklärt einen Link indem man Text in eckige Klammern [] schließt, und nun danach wieder danach in der runden Klammer #Text, also die Referenz des internen Links aufruft. Damit kann man nun sofort zu dem Heading springen, wenn man auf den erstellten internen Link drückt!

Wir können also an den Anfang eine kleine Auflistung unserere Überschriften machen und direkt erlauben auf diese Aufzurufen, quasi wird ein kleiner Table of Contents erstellt!

<!--- Version 8 hier einfügen? -->

[]: # (Bild hier einfügen?)

`r blogdown::shortcode_open("detail_disclosure", "Zeig mir die Lösung an")`
<pre>
## Version 8 des Miniberichts

#### **Übersicht**

- [Datensatz](#Datensatz)
- [Deskriptivstatistik](#Deskriptivstatistik)
- [Durchführungsplan](#Durchführung)
- [Voraussetzungsprüfung](#Vorraussetzungsprüfung)
- [Ergebnis](#Ergebnis)

#### **Datensatz** {#Datensatz}
Der Datensatz kann mit folgendem Befehl geladen werden:
```
load(url('https://pandar.netlify.app/post/fb22.rda')) 
```

#### **Deskriptiv** {#Deskriptivstatistik}

Von der Stichprobe dieses Fragebogen wurde nur die Geschlechterverteilung aufgenommen, welche folgendermaßen aussieht:

| Weiblich | Männlich | Anderes |
|---|---|---|
| 125  | 23 | 1 |

***

Der Code dazu sah folgenderweise aus:
```
fb22$geschl_faktor <- factor(fb22$geschl
                             ,levels = 1:3,
                             labels = c("weiblich"
                                        ,
                                        "männlich", "anderes"))
table(fb22$geschl_faktor)
```

#### **Was wurde durchgeführt?** {#Durchführung}

Es wurde eine lineare Regression durchgeführt, um zu sehen, ob innerhalb des Datenatz die Nerdigkeit einen linearen Zusammenhang mit der Neurotizität aufzeigt.  

Als Hypothesen formuliert:

>
>Hypothesen
>
> - H1: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist statistisch bedeutsam. 
>
> - H0: Das Regressionsgewicht für den Prädiktor Nerdigkeit ist nicht signifikant von Null verschieden. Der Zusammenhang von Nerdigkeit und Neurotizität ist nicht statistisch bedeutsam. 
>

***

Der Code dazu sah folgenderweise aus:
```
model <- lm(neuro ~ nerd, fb22)
```

#### **Vorraussetzungsprüfung** {#Vorraussetzungsprüfung}

Die Vorrausssetzungsprüfungen wurden graphisch durchgeführt.

Die Voraussetzung der Linearität:

![Voraussetzung 1](Plots_Bilder/Linearity.png)

Die restlichen Vorraussetzungsprüfungen:

![Voraussetzung 2-4](Plots_Bilder/Plots_Overview.png)

Sie sind alle annehmbar, wobei die Normalverteilung noch zusätzlich mit einem shapiro Test (p = .0698) überprüft wurde, da der dargestellte QQ-Plot schon an der Grenze zu liegen scheint.

***

Der Code dazu sah folgenderweise aus:
```
UV <- fb22$nerd
AV <- fb22$neuro

## Linearität
plot(UV, AV, xlab = "Nerdigkeit", ylab = "Neurozität", 
     main= "Zusammenhang zwischen Gewicht und Treibstoffeffizienz"
     , pch =19)
lines(loess.smooth(UV, AV), col = 'blue')

## Varianzhomogenität
# Siehe Plot
par(mfrow = c(2, 2)) #vier Abbildungen gleichzeitig
plot(model)

## Normalverteilung der Residuen
shapiro.test(model[["residuals"]])
# +plot

## Unabhängigkeit der Residuen
# Siehe plot

```

#### **Ergebnis** {#Ergebnis}

Das Ergebnis ist, dass unser Prädiktor nicht signifikant von Null verschieden ist (p-Wert von 0.3406 bei einem Alpha von .05). Die Alternativhypothese wird verworfen und die Nullhypothese bleibt erhalten.

***

Der Code dazu sah folgenderweise aus:
```
summary(model)$r.squared
p = 0.3406
p < 0.05
```
</pre>
`r blogdown::shortcode_close("detail_disclosure")`


## Fazit zu Basic Markdown

Markdown selbst bietet eher grundlegende Funktionen um einen formatierten Text darzulegen, aber die wahre Stärke von Markdown liegt in der Erweiterbarkeit des Grundgerüsts von Markdown durch verschiedene Compiler, weitere Programmiersprachen und andere externe Programme und Formate, die Markdown als Grundlage verwenden.


## Markdown und andere Formate {#Advanced .anchorheader}

Markdown kann unter anderem zum Erstellen von Websites genutzt werden, was auch einst der Grund für die Erfindung von Markdown war. Dabei lassen sich zusätzlich CSS und HTML nutzen, um weitaus komplexere Anpassungen durchzuführen. Ein Großteil von PandaR besteht auch nur aus Markdown und gelegentlichem HTML!
Aber auch für Notizen-Apps kann Markdown interessant sein, besonders wenn man etwa [Obsidian](https://obsidian.md/) benutzt, welches grundlegend mit Markdown arbeitet.
Auch Projektdokumentationen, Präsentations-Slides und PDF-Dokumente lassen sich mit Markdown anfertigen und um weitere Features erweitern die speziell für diese Formate geeignet sind.
Im nächsten Tutorial wird ein Compiler aufgeführt, der viele dieser Funktionen auch umsetzen kann, Pandoc. Dort werden auch weiter Funktionen vorgestellt, die unseren Miniaturbericht noch besser gestalten können.

RMarkdown hingegen dient spezifisch der visuellen und schriftlichen Dokumentation von Analysen in R und wird in dem letzten Tutorial dieser Reihe besprochen, da es alle vorigen Elemente von Markdown, Pandoc und R kombiniert und die finale Version des Berichts abbilden wird.

<!-- Ich glaube, viele könnten sich unter Compiler nichts vorstellen und es klingt nach zu viel extra Arbeit - hier sollte eine oberflächliche Beschreibung dabei stehen, die die Angst nimmt. -->
<!-- Dann auf jeden Fall die weitern Tutorials verlinken, wenn du auf sie Bezug nimmst. -->

***
